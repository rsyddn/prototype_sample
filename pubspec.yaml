name: prototype_sample
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_screenutil: ^5.0.0+2
  get: ^4.3.8
  get_it: ^7.1.3
  dio: ^4.0.1
  hive: ^2.0.4
  lottie: ^1.2.1
  path_provider: ^2.0.5
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  hive_generator: ^1.1.1
  build_runner: ^2.1.4

flutter:

  uses-material-design: true

  assets:
    - assets/lottie/